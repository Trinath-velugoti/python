is_valid_password=lambda password:len(password)>=8 and any(c.isdigit() for c in password) and any(c.isalpha() for c in password)
password="abc12322"
if is_valid_password(password):
    print("valid")
else:
  print("not valid")
